apiVersion: v1
kind: Secret
metadata:
  name: probe-gateway-auth
  namespace: ticket-monitor
type: Opaque
stringData:
  # 生产环境请替换为强随机字符串，例如:
  #   openssl rand -hex 32
  # 切勿将真实密钥提交到仓库。
  token: "CHANGE_ME"
