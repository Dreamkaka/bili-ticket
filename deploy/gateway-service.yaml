apiVersion: v1
kind: Service
metadata:
  name: ticket-gateway
  namespace: ticket-monitor
  labels:
    app: ticket-gateway
spec:
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  selector:
    app: ticket-gateway
  type: ClusterIP
